---
import Link from "./Link.astro";

const { partner } = Astro.props;
---

<article class="partner-card">
  <div class="card-body">
    {
      partner.logo && (
        <div class="logo-container">
          <img src={partner.logo.src} alt={partner.name} class="partner-logo" />
        </div>
      )
    }

    <h3 class="partner-name">{partner.name}</h3>
    <p class="tagline">{partner.tagline}</p>
    <p class="description">{partner.description}</p>

    <div class="link-container">
      <Link href={partner.url} target="_blank">Visit Website</Link>
    </div>
  </div>
</article>

<style>
  .partner-card {
    height: 100%;
    background-color: var(--color-background-tertiary);
    border-radius: 0.8rem;
    transition: transform 0.3s ease;
  }

  .partner-card:hover {
    transform: translateY(-0.5rem);
  }

  .card-body {
    padding: 2rem;
    height: 100%;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .logo-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 4rem;
    margin-bottom: 1rem;
  }

  .partner-logo {
    max-width: 100%;
    max-height: 4rem;
    width: auto;
    height: auto;
  }

  .partner-name {
    margin: 0;
    font-size: 1.8rem;
    color: var(--color-primary);
  }

  .tagline {
    margin: 0;
    font-size: 1.2rem;
    font-style: italic;
    color: var(--color-gray);
  }

  .description {
    margin: 0;
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-primary);
    flex: 1;
  }

  .link-container {
    margin-top: auto;
    padding-top: 1rem;
  }
</style>
