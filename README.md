# teambespin.io Website

The team be|spin homepage available at <https://teambespin.io>

## Run locally

1.  Run `npm install`
1.  Run `npm run serve`

## Build

1.  Run `npm install`
1.  Run `npm run build`
